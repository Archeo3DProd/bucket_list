<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <main>
      <div class="content-wrapper">
        <h1>Results for "<%= @search_term %>"</h1>
      </div>
      <div class="content-wrapper">
        <ul class="bare-list float-grid css-grid">
            <% ["Eat a cheese fondue on the top of the Matterhorn",
                "Build a swimming pool in the desert",
                "Take a walk in Mexico City and come back alive"].each do |idea_title| %>
              <li>
                <%= render(partial: 'idea_card', locals: { card_title: idea_title }) %>
              </li>
            <% end %>
        </ul>
      </div>
      <div class="see-more">
        <button class="btn" type="button" data-more="true">See more</button>
      </div>
    </main>

  </body>
</html>
